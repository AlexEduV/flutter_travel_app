import 'package:flutter/cupertino.dart';

class ProfileItemPage extends StatelessWidget {
  const ProfileItemPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text('Profile Item Page'),
    );
  }
}
