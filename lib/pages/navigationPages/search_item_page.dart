import 'package:flutter/cupertino.dart';

class SearchItemPage extends StatelessWidget {
  const SearchItemPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text('Search Item Page'),
    );
  }
}
